<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
  http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
  http://www.springframework.org/schema/security
  http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <!-- ログイン後にindex.htmlまたはlogin.htmlに行くと403になるのでtopに飛ばす(今回403になるのはこのパターン以外ないのでとりあえず) -->
	<http auto-config="true" use-expressions="true" access-denied-page="/top.html">
		<form-login login-page="/login.html"  default-target-url="/top.html" authentication-failure-url="/login.html?error=true" always-use-default-target="true" />
		<logout logout-success-url="/index.html" />
		<intercept-url pattern="/**" access="permitAll" />
	</http>

	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
   				users-by-username-query="SELECT name, password, TRUE FROM user WHERE name = ?;"
 				authorities-by-username-query="SELECT name, 'ROLE_USER' FROM user WHERE name = ?;"/>
		</authentication-provider>
	</authentication-manager>

<!--
	<beans:bean id="digestFilter"
		class="org.springframework.security.web.authentication.www.DigestAuthenticationFilter">
		<beans:property name="userDetailsService" ref="userService" />
		<beans:property name="authenticationEntryPoint" ref="digestEntryPoint" />
	</beans:bean>
-->

	<beans:bean id="digestEntryPoint"
		class="org.springframework.security.web.authentication.www.DigestAuthenticationEntryPoint">
		<beans:property name="realmName"
			value="Contacts Realm via Digest Authentication" />
		<beans:property name="key" value="acegi" />
	</beans:bean>

</beans:beans>